import fmDAPI from "../src";
import { generateSchemas } from "../src/codegen";

// const client = fmDAPI({});
test("generateSchemas", async () => {
  console.log("hello I'm a test");
  //   await generateSchemas();
});
